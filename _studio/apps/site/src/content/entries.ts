/* Papers, talks and projects. Adding one is adding an object here; the section
   renders itself from the array and hides while the array is empty.

   Deliberately empty. The live site points at the Google Sites record instead
   of duplicating it, and inventing entries here would put unverified venues and
   years on the page. Fill these in from the real record, one object each. */

import type { Entry } from './types.ts';

export const research: Entry[] = [];

export const talks: Entry[] = [];

export const projects: Entry[] = [];
