=== CP-ES : Task 1 (unlimited-register critical path) + Task 2 ===
BASELINE carried in: cpen_best.qasm width 18, opt2 depth 258, 520 cx,
580 u3, verify_sv18 PASS err 6.11e-15.  Op list cpen_search_11.pkl,
422 ops (184 x, 112 cx, 94 ccx, 12 c3x, 12 c3x_dg, 7 cz, 1 ccz).

MODEL DEFINITIONS (cpes_task1.py), all on cpae_core.PROF touch offsets
(rccx target 1..7 / ctrl0 at 4 / ctrl1 at 2,6; rcccx target 1..13;
ccz 1..10; cz 1..3; cx 1; x 0 layers).  Delay on a producer->consumer link
= last_touch_offset(producer on that wire) - first_touch_offset(consumer).
  M0  18 physical wires, RAW+WAR+WAW, heuristic list schedule.
  M1  cpen_edge2.dag3 free=all18 (the previously reported 165): drops every
      same-target edge, then list-schedules on 18 PHYSICAL wires.
  M1b M1's DAG with the RAW obligation repaired (a reader of a wire depends
      on EVERY unread writer of it, not only the last).
  M2  SSA / unlimited registers.  Every write creates a fresh register, so
      WAR and WAW vanish; an op depends on the producer of each control
      value AND on the producer of the OLD TARGET value (in-place update of
      a live value is genuine).  Free fan-out, no wire contention =>
      strict LOWER BOUND on any schedule/allocation of this op list.
  M2r M2 with fan-out charged (readers of one value serialise on its wire).

MEASURED (cpes_task1.py)
  op list              M0    M1    M1b   M2    M2r
  cpen_start.pkl  418  240   165   246   167   176
  cpen_search_11  422  242   175   236   169   174
=> the reported 165 REPRODUCES exactly, but on cpen_start.pkl (418 ops),
   not on the banked cpen_search_11.pkl (422 ops), where the same code
   gives 175.
=> M1's DAG is unsound as a bound: when a wire is "free" it keeps only the
   LAST writer as the RAW predecessor of a later reader, so contributions
   from earlier co-writers are dropped; and it is evaluated by a heuristic
   list schedule on 18 physical wires, not by a critical path.
=> CORRECTED unlimited-register critical path of the banked op list,
   in-place update genuine, free fan-out: 169 (167 for cpen_start).
   M0 - M2 = 242 - 169 = 73 layers are wire reuse; 169 layers are not.

MEASURED (cpes_model2.py) cpae_core.model_depth is off by one per op:
  model_depth([cx]) = 0, [cx,cx] = 0, [cx]*3 = 0, [ccx] = 6, [c3x] = 12.
  Corrected convention (lay = layers used; op occupies S+1..S+last):
  1cx 1, 2cx 2, 3cx 3, 1ccx 7, 2 same-target ccx 14, 1c3x 13.
  op list            PROF conv (M0/M2)   corrected conv (M0/M2)   real opt2
  cpen_start   418      240 / 167            305 / 212               -
  cpen_search_11 422    242 / 169            311 / 213              258
=> the two conventions bracket the real depth (242 < 258 < 311).  The
   unlimited-register floor is 169 in the repo's convention and 213 in the
   corrected one; calibrating by the same offsets puts the REAL
   unlimited-register floor at ~185-215.  Both are ABOVE 150.

CONCLUSION TASK 1 (measured): the reported 165 is reproducible only on the
418-op predecessor with an unsound DAG.  Corrected number for the BANKED
op list, model = SSA/unlimited registers, in-place update of a live value
GENUINE (an op depends on the producer of each control value and on the
producer of the old target value), WAR/WAW dropped, free fan-out, PROF
touch offsets, i.e. a strict LOWER BOUND on every schedule and every
register allocation of this op list: 169.
=> 169 > 150, so THIS OP LIST CANNOT REACH DEPTH < 150 BY ANY SCHEDULING
   OR REGISTER ALLOCATION.  Only changing the op list can.

MEASURED (cpes_chain.py) composition of that 169-layer chain (42 ops,
16 ccx, 9 cx, 8 x, 5 c3x, 4 c3x_dg; target wires q3 x13, q4 x9, q17 x7):
  ACC-FREE       (consecutive same-target writes, no reader between)  78
  RAW-control    (a value feeding a later op's control)               37
  ACC-mirror     (compute/uncompute of the same wiring, forced)       30
  ACC-read-pinned(a reader sits between the two writes)               12
  + 12 for the last op's own latency.
MEASURED (cpes_reassoc.py) whole-list census of consecutive same-target
write pairs: 194 freely commuting, 147 read-pinned, 55 mirror/self-inverse.
=> the only lever with headroom on the chain is the 78 ACC-FREE layers,
   and taking them needs extra registers and extra uncompute ops.

MEASURED (cpes_m2e.py) the brief's OWN artefact definition applied exactly:
an op's edge to the producer of its old target value is dropped exactly when
that old value is identically 0 on all 4096 inputs (reset ancilla reused for
a different value); every other in-place update keeps its edge.
  op list           M2 (in-place always genuine)   M2e (brief's definition)
  cpen_start   418        167 / 212corr                146 / 195corr
  cpen_search_11 422      169 / 213corr                148 / 197corr
  (30 of 422 writes land on an all-zero wire.)

MEASURED (cpes_epoch.py, cpes_obj.py, cpes_run_epoch.py) the same relaxation
at the REAL metric, semantically valid (pure register allocation):
  every maximal nonzero interval of every ancilla given its OWN wire
  -> 48 wires, 30 extra registers, SAME 422 ops
  real emit objective (cpen_obj pipeline): depth 258, cx 527
  baseline at 18 wires, same pipeline:     depth 258, cx 520
  plain transpile(opt2) of the op list:    278 at 18 wires AND 278 at 48
=> UNLIMITED ANCILLA REGISTERS BUY EXACTLY 0 REAL LAYERS (258 -> 258, +7 cx).
   The 165 / 169 / 148 model numbers are artefacts of the op model's touch
   arithmetic; at the real metric the unlimited-register depth of this op
   list is 258, identical to the 18-wire depth.

MEASURED (cpes_profile.py) composition of the banked 258-layer circuit
(transpile opt2 of cpen_best.qasm, ASAP layering):
  depth 258 = 146 cx layers + 112 pure-u3 layers on the critical path.
  520 cx spread over 227 layers; cx-per-layer histogram
    1:79  2:60  3:43  4:35  5:8  6:2   (18 wires allow 9)
  31 layers carry no cx at all; mean wire occupancy 6.28 of 18.
  Leader for comparison: 137 depth / 561 cx = 4.1 cx per layer; ours 2.3.

MEASURED (cpes_nldepth / cpes_nlchain) structural chain length under the
brief's artefact rule: the longest genuine-dependency chain carries
26 Toffoli-class ops (33 RCCX-equivalents counting RC3X as 2) out of 118.
Measured real cost of k serial RCCX in a target->control chain
(transpile opt2): k=15 -> 91, 19 -> 115, 20 -> 121, 22 -> 133, 24 -> 145,
26 -> 157, 30 -> 181.  So <=24 serial Toffoli-class ops is the most a
depth-150 circuit can carry; this op list carries 26 (33 equivalents).
(CAVEAT, measured: emitting the chain ops ALONE at 18 wires gives only 65-90
because adjacent compute/uncompute pairs then annihilate -- that figure is
not a bound and was discarded.)

MEASURED (cpes_floor.py / cpes_floor2.py) REAL-METRIC LOWER BOUND.
Relaxation: take a genuine-dependency chain; for each consecutive pair keep
ONLY the wire that carries the dependency, in the same role on both sides;
give every other operand its own fresh wire.  This removes every other wire
conflict and makes all wirings distinct so no gate can cancel, so its
transpiled depth lower-bounds every schedule and every register allocation
of the full op list.
  chain chosen by Toffoli-op count : 46 ops, 71 wires -> opt2 158, opt3 158
  chain chosen by gadget weight    : 51 ops, 78 wires -> opt2 174, opt3 174
  chain chosen by op count         : 60 ops        -> opt2 126
=> REAL-METRIC FLOOR OF THIS OP LIST: 174 (opt2 = opt3).
   Banked real depth 258.  Target 150.  174 > 150.

TASK 1 ANSWER (measured, three independent ways)
  1. op model, brief's artefact definition, unlimited registers: 148
     (PROF convention) / 197 (off-by-one-corrected convention).
  2. real metric, semantically valid unlimited-ancilla relaxation
     (48 wires, every ancilla epoch its own register): 258 = the 18-wire
     depth, i.e. registers buy ZERO real layers.
  3. real metric, maximally relaxed genuine-dependency chain: 174.
  The previously reported 165 is none of these: it reproduces only on the
  418-op predecessor with a DAG that drops RAW obligations, and is not a
  bound on anything.
=> THE UNLIMITED-REGISTER FLOOR IS ABOVE 150 (174 at the real metric).
   THIS OP LIST CANNOT REACH DEPTH < 150 BY ANY SCHEDULING OR REGISTER
   ALLOCATION.  Task 2 therefore cannot be an incremental rescheduling;
   it requires removing Toffoli-class ops FROM THE CHAIN (26 -> <= 24
   equivalents), not moving them between wires.

=== TASK 2 : is the 258 register pressure or chain length? (real metric) ===
EXACT TRANSFORMATION (cpes_host.py, replay-verified on all 4096 inputs):
every interval in which a DATA wire d holds something other than its raw bit
is moved onto a fresh wire u by  cx(d,u) ... [refs to d redirected to u] ...
cx(d,u).  u starts and ends |0>, d is never touched, so the circuit is
exactly equal.  63 such hosting epochs exist in the banked op list.
MEASURED (real emit objective = cpen_obj pipeline, the one that produced 258):
  baseline                                    18 wires  258 / 520 cx
  every ancilla epoch its own wire            48 wires  258 / 527 cx
  all data-wire hosting moved off (63 epochs) 81 wires  255 / 666 cx
  both relaxations together                  111 wires  209 / 669 cx
  (all four exact; the last three replay-verified wire by wire)
MEASURED per-data-wire unhosting (one wire at a time, each epoch its own
fresh wire; all exact):
  q0 258/25w  q1 258/21w  q2 258/24w  q3 258/24w  q4 259/23w  q5 258/23w
  q6 263/20w  q7 258/22w  q8 258/21w  q9 258/21w  q10 258/28w  q11 257/27w
=> no single wire's hosting is the bottleneck; register pressure is diffuse.
=> 93 EXTRA REGISTERS (111 wires) AND +149 CX BUY 49 LAYERS AND STOP AT 209.
   Combined with the 174 chain lower bound, the unlimited-register real
   depth of this op list is between 174 and 209.  The 150 target is below
   both.  Nothing below 258 was produced; cpes_best.qasm not created.
   Baseline re-verified this session: cpen_best.qasm verify_sv18 PASS
   err 6.110262476351803e-15, opt2 258/520, opt3 258/520.

WHAT A DEPTH-150 CONSTRUCTION WOULD HAVE TO MEET (measured, not assumed):
  - at most ~24 Toffoli-class ops on its longest genuine-dependency chain
    (measured: 24 serial RCCX transpile to 145, 26 to 157); this op list
    has 26 ops = 33 RCCX-equivalents on that chain.
  - at least ~4 cx per layer sustained (leader: 561 cx / 137 depth = 4.1;
    this circuit: 520 / 258 = 2.0, ASAP histogram max 6, mean 2.3 over the
    227 layers that carry any cx, 31 layers carry none).

MEASURED (mirror structure of the genuine chain): of the 26 Toffoli-class ops
on the longest genuine-dependency chain, 12 are uncomputes whose compute
partner is ALSO on the chain (6 compute/uncompute pairs); 20 distinct AND
values are represented; kinds 22 ccx + 2 c3x + 2 c3x_dg.
=> at most 6 chain levels could ever be recovered by taking uncomputes off
   the chain, and the register relaxation that does exactly that (111 wires)
   measured 209, not the ~121 that 20 serial RCCX would cost -- so the 209
   is bound by the rest of the stream, not by this chain alone.
